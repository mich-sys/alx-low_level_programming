#!/bin/bash
#include <studio.h>

/**
*main - prints the phrase "programming is like building a multilinger puzzle
*Return: 0 if exited properly, non-zero otherwise
*/
int mail(void)
{
    puts("/"programming is like building a multilingual puzzle");
    return (0);
}
